#include<stdio.h>
#include<string.h>

int main(){

    //strcat() to concatenate two strings
    //strlen() to show length of a string
    //strrev() to show reverse of a string
    //strcpy() to copy one string to another
    //strcmp() to compare two strings

    // char str[]="harry";
    // char str2[]="potter";
    // char str3[54];

    //we cannot do that: str3 = strcat(s1,s2);
    //instead we can do this strcpy(s3, strcat(s1,s2))

    //strcmp() result depends upon compiler some written ASCII difference while some write negative and positive values

    //if in strcmp() a value matches it returns 0

    return 0;
}